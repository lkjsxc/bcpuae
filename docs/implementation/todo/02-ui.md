# Phase 2: UI Infrastructure

State management and rendering.

## Files to Create

### `src/ui/theme.rs` (~48 lines)
- [ ] Define `Theme` struct with colors
- [ ] Implement `default()`
- [ ] Define notification color mapping

### `src/ui/layout.rs` (~42 lines)
- [ ] Define `Layout` struct with Rects
- [ ] Implement `calculate()` for terminal size
- [ ] Define constraints (editor, notifications, status)

### `src/ui/state.rs` (~106 lines)
- [ ] Define `EditorState` struct
- [ ] Implement `new()`, `open()`
- [ ] Implement `move_cursor()`, `move_start_of_line()`, `move_end_of_line()`
- [ ] Implement `set_mode()`
- [ ] Implement `scroll_to_cursor()`

### `src/ui/renderer.rs` (~175 lines)
- [ ] Define `Renderer` struct
- [ ] Implement `new()`
- [ ] Implement `setup_terminal()`
- [ ] Implement `restore_terminal()`
- [ ] Implement `draw()`
- [ ] Implement draw_editor(), draw_notifications(), draw_status_bar()

### `src/ui/mod.rs` (~20 lines)
- [ ] Declare modules
- [ ] Re-export public types

## Acceptance Criteria

- [ ] Terminal setup succeeds in TTY
- [ ] Layout calculates correctly for 80x24
- [ ] Line numbers render in gutter
- [ ] Status bar shows mode
- [ ] All files ≤200 lines

## Known Issues to Watch

- `scroll_to_cursor()` needs actual layout dimensions (not hardcoded 80x24)
- Terminal restore must work even on panic
